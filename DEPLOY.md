# Déploiement TelcoManager

Document de référence : `docs/DEPLOYMENT.md`

Ce fichier racine est conservé comme point d’entrée court pour éviter les doublons.

## Démarrage rapide

```bash
cd /home/nath-admin/.openclaw/workspace/TelcoManager
docker compose up -d --build
```

Application web : `http://localhost:8081`

## Identifiants par défaut

- Username : `admin`
- Password : `admin123`

## Référence complète

Consulte `docs/DEPLOYMENT.md` pour :
- configuration `.env`
- maintenance
- sauvegardes
- publication et exploitation
